using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Eraser : MonoBehaviour
{
    public static Eraser instence;
    void Start()
    {
        instence = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
